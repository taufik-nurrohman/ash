<!DOCTYPE html>
<html class dir="<?= $site->direction; ?>">
  <head>
    <meta charset="<?= $site->charset; ?>">
    <title><?= $t; ?></title>
    <link href="<?= $url; ?>/favicon.ico" rel="icon">
    <style>
    :root {
      background: #fff;
      color: #000;
    }
    </style>
  </head>
  <body>
    <!-- Comment with some PHP <?= PHP_VERSION; ?> expression. -->
    <h1>
      <?= $page->title; ?>
    </h1>
    <main>
      <?php foreach ($pages as $page): ?>
      <article id="<?= $page->id; ?>">
        <h2>
          <a href="<?= $page->url; ?>">
            <?= $page->title; ?>
          </a>
        </h2>
        <?= $page->content; ?>
      </article>
      <?php endforeach; ?>
    </main>
    <script>
    console.log('Yo!');
    </script>
  </body>
</html>
